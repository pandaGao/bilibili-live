var webpack = require('webpack')
var merge = require('webpack-merge')
var base = require('./webpack.config.js')

module.exports = merge(base, {
  output: {
    filename: "bilibili-live.min.js",
  },
  plugins: [
  new webpack.optimize.UglifyJsPlugin({})
]
})
